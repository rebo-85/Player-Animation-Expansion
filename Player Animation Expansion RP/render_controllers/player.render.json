{
  "format_version": "1.8.0",
  "render_controllers": {
    "controller.render.rebo.player.first_person": {
      "geometry": "Geometry.default",
      "materials": [ { "*": "Material.default" } ],
      "textures": [ "Texture.default" ],
      "part_visibility": [
        { "*": true },
        { "head": false }
        // ,
        
        // { "rightArm": "q.is_item_name_any('slot.weapon.mainhand', '') || q.is_item_name_any('slot.weapon.mainhand', 0, 'minecraft:map')" },
        // { "leftArm": "q.is_item_name_any('slot.weapon.offhand', '') || q.is_item_name_any('slot.weapon.offhand', 0, 'minecraft:map')" }

        // { "rightArm": "!v.is_holding_right || q.is_item_name_any('slot.weapon.mainhand', 0, 'minecraft:map')" },
        // { "leftArm": "!v.is_holding_left || q.is_item_name_any('slot.weapon.offhand', 0, 'minecraft:map')" }

        // { "rightArm": "query.get_equipped_item_name(0, 1) == '' || query.get_equipped_item_name(0, 1) == 'map'" },
        // { "rightSleeve": "query.get_equipped_item_name(0, 1) == '' || query.get_equipped_item_name(0, 1) == 'map'" },
        // { "leftArm": "(query.get_equipped_item_name(0, 1) == 'map' && query.get_equipped_item_name('off_hand') != 'shield') || (query.get_equipped_item_name('off_hand') == 'map' && !query.item_is_charged) || (!query.item_is_charged && (v.item_use_normalized > 0 && v.item_use_normalized < 1.0))" },
        // { "leftSleeve": "(query.get_equipped_item_name(0, 1) == 'map' && query.get_equipped_item_name('off_hand') != 'shield') || (query.get_equipped_item_name('off_hand') == 'map' && !query.item_is_charged) || (!query.item_is_charged && (v.item_use_normalized > 0 && v.item_use_normalized < 1.0))" }
      ]
    }
  }
}
